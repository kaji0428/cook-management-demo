# === Gradle（使っている場合）===
.gradle/
build/
# ※ Gradle の wrapper はコミットOK: gradlew, gradlew.bat, gradle/wrapper/*
# 環境依存の秘密が入ることがある
gradle.properties

# === Spring Boot の設定（秘密除外）===
# 実運用の値はコミットしない
src/main/resources/application*.yml
src/main/resources/application*.properties
.env
.env.*
.env.*.local

# 代わりに「例」だけコミットする（除外ルールの打ち消し）
!src/main/resources/application-example.yml

# === DB/ローカルファイル ===
*.h2.db
*.mv.db
*.db

# === 証明書/鍵類 ===
*.jks
*.p12
*.keystore

# === ログ ===
logs/
*.log
